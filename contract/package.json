{
  "name": "@orvano/contract",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The Orvano API contract in TypeSpec. `pnpm build` compiles it to dist/openapi.json (spec 0001).",
  "scripts": {
    "build": "tsp compile ."
  },
  "devDependencies": {
    "@typespec/compiler": "1.16.0",
    "@typespec/http": "1.16.0",
    "@typespec/openapi": "1.16.0",
    "@typespec/openapi3": "1.16.0"
  }
}
