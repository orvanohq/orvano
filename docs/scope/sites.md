# Epic: Sites

Host the frontends built on Orvano next to their backend. See [index.md](index.md) for the full plan.

### 29. Sites hosting · needs a decision
Deploy static sites and server rendered Next.js apps, plus Flutter web builds, from the CLI or Git, with preview URLs per branch and instant rollback.
**Done when:** a Next.js app with server rendering and a Flutter web app both deploy from Git, get a preview URL, and roll back to the previous version in one click.
- [ ] Design it (spec): `/architect sites hosting`

### 30. Custom domains & TLS · needs a decision
Attach your own domains to sites and functions with automatic certificates and renewal.
**Done when:** you add a domain in the console, follow the DNS steps shown, and it serves over HTTPS with a certificate that renews on its own.
- [ ] Design it (spec): `/architect custom domains & TLS`
