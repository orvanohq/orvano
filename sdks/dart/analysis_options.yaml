# Shared by every Orvano Dart package (spec 0001). Strict casts and types, like TS `strict`.
include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    - public_member_api_docs
    - prefer_final_locals
    - prefer_single_quotes
    - unawaited_futures
