// Generated by Orvano SdkGen. Do not edit.
import type { Client, RequestOptions } from '../runtime/client.js'
import type { Health } from './models.js'

/** Operations in the `health` service. */
export class HealthService {
  readonly #client: Client

  constructor(client: Client) {
    this.#client = client
  }

  /** Checks that the server is up and returns its version. */
  get(options?: RequestOptions): Promise<Health> {
    return this.#client.request<Health>({ method: 'GET', path: '/v1/health' }, options)
  }
}

/** Every service in this entry, on one object: `orvano.health.get()`. */
export class Orvano {
  /** The client every service sends through. */
  readonly client: Client
  /** Operations in the `health` service. */
  readonly health: HealthService

  constructor(client: Client) {
    this.client = client
    this.health = new HealthService(client)
  }
}
