namespace Orvano.Server.Hosting;

/// <summary>
/// Test only seed data for the shared scenarios (<c>tests/scenarios/fixtures.yaml</c>). The file's
/// shape waits for the platform data model (scope row 3); until then only its presence is checked.
/// </summary>
internal static class TestFixtures
{
    public const string Setting = "ORVANO_TEST_FIXTURES";
}
