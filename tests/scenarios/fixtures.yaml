# Seed data for the shared scenarios, loaded by a server started with
# ORVANO_TEST_FIXTURES=<path to this file> in the Test environment (spec 0001).
# Its full shape waits for the platform data model (scope row 3). Until then it holds only the
# console session tokens the server accepts; the JS runner sends the first for `as: console` steps.
consoleSessions:
  - test-console-session
