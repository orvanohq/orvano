// Generated by Orvano SdkGen. Do not edit.

/// Error codes only the test operations send.
abstract final class TestErrorCode {
  /// `test_conflict`
  static const testConflict = 'test_conflict';
}

/// The answer to `test.consolePing`.
final class TestConsolePing {
  /// Creates a [TestConsolePing].
  const TestConsolePing({required this.status});

  /// Decodes a [TestConsolePing] from JSON.
  factory TestConsolePing.fromJson(Map<String, dynamic> json) =>
      TestConsolePing(status: json['status'] as String);

  /// Always `ok`.
  final String status;

  /// Encodes this [TestConsolePing] as JSON.
  Map<String, dynamic> toJson() => {'status': status};
}

/// One fixed item in the `test.list` page.
final class TestItem {
  /// Creates a [TestItem].
  const TestItem({required this.id});

  /// Decodes a [TestItem] from JSON.
  factory TestItem.fromJson(Map<String, dynamic> json) =>
      TestItem(id: json['id'] as String);

  /// `item-1` to `item-5`.
  final String id;

  /// Encodes this [TestItem] as JSON.
  Map<String, dynamic> toJson() => {'id': id};
}

/// One page of `test.list`.
final class TestItemPage {
  /// Creates a [TestItemPage].
  const TestItemPage({required this.items, this.nextCursor});

  /// Decodes a [TestItemPage] from JSON.
  factory TestItemPage.fromJson(Map<String, dynamic> json) => TestItemPage(
    items: (json['items'] as List<dynamic>)
        .map((e) => TestItem.fromJson(e as Map<String, dynamic>))
        .toList(),
    nextCursor: json['nextCursor'] == null
        ? null
        : json['nextCursor'] as String,
  );

  /// The items on this page.
  final List<TestItem> items;

  /// Pass it as `cursor` to get the next page; null on the last page.
  final String? nextCursor;

  /// Encodes this [TestItemPage] as JSON.
  Map<String, dynamic> toJson() => {
    'items': items.map((e) => e.toJson()).toList(),
    'nextCursor': nextCursor,
  };
}

/// The payload of the `test.pinged` event.
final class TestPinged {
  /// Creates a [TestPinged].
  const TestPinged({required this.message, required this.at});

  /// Decodes a [TestPinged] from JSON.
  factory TestPinged.fromJson(Map<String, dynamic> json) => TestPinged(
    message: json['message'] as String,
    at: DateTime.parse(json['at'] as String),
  );

  /// Any text.
  final String message;

  /// When the ping happened.
  final DateTime at;

  /// Encodes this [TestPinged] as JSON.
  Map<String, dynamic> toJson() => {
    'message': message,
    'at': at.toUtc().toIso8601String(),
  };
}
