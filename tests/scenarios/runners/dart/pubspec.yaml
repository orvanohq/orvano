name: orvano_scenarios
description: Runs the shared Orvano scenarios through orvano_core and orvano_dart (spec 0001).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  orvano_core: ^0.0.0
  orvano_dart: ^0.0.0
  yaml: ^3.1.4

dev_dependencies:
  lints: ^6.1.0
