import 'package:flutter/widgets.dart';

// The app itself does nothing; integration_test/scenarios_test.dart drives the scenarios.
void main() => runApp(
  const Center(
    child: Text('Orvano scenarios', textDirection: TextDirection.ltr),
  ),
);
