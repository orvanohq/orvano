// Generated by Orvano SdkGen. Do not edit.
import type { TestConsolePing } from './test-models.js'
import { Orvano } from '@orvano/console-client'
import type { Client, RequestOptions } from '@orvano/js'

/** Operations in the `test` service. */
export class TestService {
  readonly #client: Client

  constructor(client: Client) {
    this.#client = client
  }

  /** Answers `{ status: ok }` to a valid console session. */
  consolePing(options?: RequestOptions): Promise<TestConsolePing> {
    return this.#client.request<TestConsolePing>(
      { method: 'GET', path: '/v1/console/test/ping' },
      options,
    )
  }
}

/** `@orvano/console-client` plus the console test services, as the scenarios call them. */
export class ConsoleSurface extends Orvano {
  /** Operations in the `test` service. */
  readonly test: TestService

  constructor(client: Client) {
    super(client)
    this.test = new TestService(client)
  }
}
