// Generated by Orvano SdkGen. Do not edit.

/** Error codes only the test operations send. */
export const TestErrorCode = {
  /** `test_conflict` */
  testConflict: 'test_conflict',
} as const

/** One of the codes in {@link TestErrorCode}. */
export type TestErrorCode = (typeof TestErrorCode)[keyof typeof TestErrorCode]

/** The answer to `test.consolePing`. */
export interface TestConsolePing {
  /** Always `ok`. */
  status: string
}

/** One fixed item in the `test.list` page. */
export interface TestItem {
  /** `item-1` to `item-5`. */
  id: string
}

/** One page of `test.list`. */
export interface TestItemPage {
  /** The items on this page. */
  items: TestItem[]
  /** Pass it as `cursor` to get the next page; null on the last page. */
  nextCursor: string | null
}

/** The payload of the `test.pinged` event. */
export interface TestPinged {
  /** Any text. */
  message: string
  /** When the ping happened. */
  at: string
}
